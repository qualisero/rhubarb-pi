{
  "name": "@rhubarb-pi/session-color",
  "version": "1.0.0",
  "description": "Pi extension that displays a colored band in the footer to distinguish sessions",
  "main": "index.ts",
  "scripts": {
    "install:global": "mkdir -p ~/.pi/agent/extensions && cp index.ts ~/.pi/agent/extensions/session-color.ts && echo 'Installed to ~/.pi/agent/extensions/session-color.ts'",
    "install:project": "mkdir -p .pi/extensions && cp index.ts .pi/extensions/session-color.ts && echo 'Installed to .pi/extensions/session-color.ts'",
    "uninstall:global": "rm -f ~/.pi/agent/extensions/session-color.ts && echo 'Uninstalled from ~/.pi/agent/extensions/'",
    "uninstall:project": "rm -f .pi/extensions/session-color.ts && echo 'Uninstalled from .pi/extensions/'"
  },
  "keywords": [
    "pi",
    "extension",
    "color",
    "session"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.0.3",
    "typescript": "^5.9.3"
  }
}
