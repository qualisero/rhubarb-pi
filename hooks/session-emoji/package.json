{
  "name": "@rhubarb-pi/session-emoji",
  "version": "1.0.0",
  "description": "Pi extension that displays a contextual emoji in pi's footer",
  "main": "index.ts",
  "scripts": {
    "install:global": "mkdir -p ~/.pi/agent/extensions && cp index.ts ~/.pi/agent/extensions/session-emoji.ts && echo 'Installed to ~/.pi/agent/extensions/session-emoji.ts'",
    "install:project": "mkdir -p .pi/extensions && cp index.ts .pi/extensions/session-emoji.ts && echo 'Installed to .pi/extensions/session-emoji.ts'",
    "uninstall:global": "rm -f ~/.pi/agent/extensions/session-emoji.ts && rm -rf ~/.pi/agent/extensions/session-emoji && echo 'Uninstalled from ~/.pi/agent/extensions/'",
    "uninstall:project": "rm -f .pi/extensions/session-emoji.ts && echo 'Uninstalled from .pi/extensions/'"
  },
  "keywords": [
    "pi",
    "extension",
    "emoji",
    "session"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.0.3",
    "typescript": "^5.9.3"
  }
}
